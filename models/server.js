// const mongoose = require('mongoose');
// const { Schema } = mongoose;

// const serverSchema = new Schema({name: String, players: {type: Number, default: 0}})

// module.exports = mongoose.model(serverSchema);